from setuptools import setup


if __name__ == "__main__":
    setup(
        install_requires=[
            "pandas",
            "numpy",
            "matplotlib",
            "torch",
            "sklearn",
            "notebook",
        ]
    )
